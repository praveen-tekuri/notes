## 1. Introduction to Node.js

- Node.js is an open-source, cross-platform JavaScript runtime environment built on Chrome's V8 JavaScript engine. It allows developers to run JavaScript on the server side, enabling the creation of scalable network applications, fast APIs, and full-stack web solutions using a single programming language.
- Key Features:
    - Event-driven, non-blocking I/O for high performance
    - Uses NPM for managing libraries and dependencies.
    - Ideal for:
        - Web servers and APIs
        - Real-time applications(chat, gaming)
        - Microservices
        - Commandline tools
- Node.js allows JavaScript to be executed outside of a web browser. this means that while JavaScript is traditionally associated with the web and browsers, Node.js extends its reach, allowing you to run JavaScript code on servers, desktops, and even embedded systems. This is why Node.js is often described with the phrase `"Run JavaScript Everywhere."`
- To run the JS, you always need an JS engine. Everywhere you write JS code, there is an JS engine, that executes the code.

## 2. JavaScript on Server
> What is a Server?
- A server is essentially a remote computer. You can think of it as a computer whose CPU works remotely. Servers can be accessed over a network to provide resources and services to other computer programs. 
- When a computer needs to communicate with a server, it sends a request to the server using its IP address. Initially, JavaScript could only be executed within web browsers, limiting its use to client-side tasks. However, with the introduction of Node.js, JavaScript can now also be executed on servers, allowing developers to use the same language for both client-side and server-side programming.

> What is IP Address?
- An IP address, or Internet Protocol address, is a unique number that identifies
every device connected to the internet.

> What is v8?
- The V8 is Google's JavaScript engine, written in C++.
- JavaScript code is executed by V8 (written in C++), which then compiles it down to machine code that the computer can execute.
- `NODEJS is a c++ application with v8 embedded into it`
- ECMAScript standards are followed by JavaScript engines like V8, SpiderMonkey, Chakra, and others to ensure consistent behavior across different environments. 
- Node.js has v8 Engine, and also has some superpowers, such as api calls on servers, which make it more powerful than v8 engines alone, which cannot do database connections, api calls, etc. because of ECMA standards. 
- We write JS(High Level Code), and then the V8 engine translates it into machine and assembly code also known as low level code, so the machine can understand it. Low-level code refers to programming languages or code that is closer to machine language and hardware. It provides little abstraction from the computer's architecture and allows for fine-grained control over system resources. 
- Here are some key points about low-level code: 
    1. Machine Language: The most basic form of low-level code, consisting of binary (0s and 1s) instructions that the computer's CPU can directly execute. 
    2. Assembly Language: A step above machine language, assembly language uses symbolic representations (mnemonics) for operations and memory addresses, making it somewhat easier for humans to read and write. Each assembly instruction corresponds to a specific machine language instruction.

## 3. Writing Code

- Download and install node.js, along with node, NPM also will be installed.
> Write Code in: Node REPL [Read, Evaluate, Print, Loop] is a very quick and easiest way to write code
- If you write node in command and hit enter, you went to a REPL. It enters into a program. now write and execute any code.
- We wrote javascript in node js environment which is very similar to console. the browser is also running v8 engine behind the scenes, the browser pass the code to v8 engine and execute the code. And similarly node JS also executes in the someway.
> Write Code in: VS Code
    ```js
    // app.js

    let name = "Node JS 03";
    let a = 5;
    let b = 10;
    let c = a + b;
    console.log(name);
    console.log(c);
    ```
- Open terminal and run command `node app.js` (use Ctrl + ~ to open the terminal in VS code ) the output will be displayed in the terminal.
- So node JS will take the app.js file, gives it to v8 engine and then the code is read line by line by v8 engine and then executed. Because js is a synchronous and single threaded line by line the code is executed and it will be running inside node.js

> Global objects in NodeJS

- In Node.js, the global object is known as `global`, which is equivalent to the `window` object in the browser(*The window object is a global object provided by the browser, not by the V8 engine.*)
- A `global` object is not a part of the V8 engine; instead, it’s a feature provided by Node.js.
- This `global` object offers various functionalities, such as `setTimeout()` , `setInterval()` , and more.
    ```js
    console.log(this); // Outputs: {} 
    ```
- When you use console.log(this); at the global level in Node.js, it will log an empty object, indicating that this does not refer to the global object in this context.
- `GlobalThis` is always a global object, regardless of where it is accessed. It was introduced in ECMAScript 2020 to provide a standardized way to refer to the global object in any environment (browsers, Node.js, etc.). 
    - In browsers, globalThis is equivalent to window object
    - In NodeJS, globalThis is equivalent to global object
    - It provides a consistent way to access the global object without worrying about the environment.

## 4. Module Export & Requirements.

- We will create multiple files which are used to build whole project. to create those files we have concept called modules. 
- To make multiple files works together, or to connect multiple files we will have `require`(one module into another module) function which is used to require other modules into your main module. 
- Module is basically a collection of code which is private to itself, it exists separately or independently of any other program.

- To call or run the module as it is in another file or module, just import the module.
- To access properties, methods or any data from a specific module, we need to explicitly export them using `module.exports` and import them using `require()` function in where you want to access them.
- we can make a folder itself a module (e.g calculate) by creating multiple files and merging them into single index.js file so the app.js will talk to calculate module.

- Whenever you create a separate module, and you require that module, the code will run, but you can’t access the variables and methods and functions of one module into another.

    ```js
    // CommonJS Modules (used in NodeJS by Default)

    // File1
    module.exports = function name / variable name // export
    
    // To export multiple things from a module, wrap it into object
    module.exports = {x:x, caluculateSum: caluculateSum}
    
    // OR
    module.exports.x = x;
    module.exports.calculateSum = calculateSum;
    
    // OR
    module.exports = { x, calculateSum};

    // File2
    const functionName = require("./otherfile.js"); // import
    const obj = require("./sum.js")
    const { x, calculateSum } = require("/sum");
    // you can omit the .js extension, and it will still work correctly. Node.js 
    // automatically resolves the file extension for you.

    obj.caluculateSum(a, b);
    ```
- `module.exports` is empty object, you're attaching the properties to the same object. Whatever you will export will be returned from `require` function. 
- From a module we can’t access its private variables and functions outside unless the module wants to be.
- The pattern of exporting and importing module is known as common JS (CJS) modules, and there is one more thing ES modules (MJS).

> Module Systems

- CommonJS modules (by default used in nodeJS)
    - module.exports & require()
    - Older way
    - Synchronous
    - non-strict mode
- ES modules (by default used in React, angular)
    - export & import
    - Newer way
    - Asynchronous
    - strict mode

- To enable ES modules, you have to configure package.json with {"type": “module”}

    ```js
    // ES Modules

    //package.json
    {
        "type": "module"
    }

    // File1
    export function functionName(){...}

    // File2
    import {functionName} from “./sum.js”;
    ```

- There are two major differences between these two module systems that are important to note:
    - **Synchronous vs. Asynchronous:** CommonJS requires modules in a synchronous manner, meaning the next line of code will execute only after the module has been loaded. In contrast, ES modules load modules asynchronously, allowing for more efficient and flexible code execution. This distinction is a powerful feature and an important point to remember for interviews. 

    - **Strict Mode:** Another significant difference is that CommonJS code runs in non-strict mode, while ES modules execute in strict mode. This means that ES modules enforce stricter parsing and error handling, making them generally safer and more reliable.

    - In a **CommonJS** module, you can define a variable without using `var , let , or const` , and the code will execute without throwing an error because it operates in non-strict mode. However, in an **ES module**, if you try to execute the same code, it will throw an error because ES modules run in strict mode. In strict mode, you cannot define variables without declaring them first.

        ```jsx
        z = "Hello world"
        ```
- Importing JSON file: Suppose you have a data.json file, and you want to import it into your JavaScript code. You can do this easily using require . 
    ```js
        // data.json
    {
        "name": "praveen",
        "role": "developer"
    }

    // app.js
    const data = require("./data.json");
    console.log(data)
    ```
- In general, a module can be a single file or a folder. A module is essentially a collection of JavaScript code that is private to itself and organized separately. If you want to export something from a module, you can use module.exports to expose the desired functionality to other parts of your application.